# AI培训智能客服 Roadmap

最后更新：2026-07-30

## 当前状态

- 当前阶段：Part 6 — 技术 Demo 已部署，进入接手阶段
- 当前目标：冻结新功能，保持 Vercel 技术 Demo 可交接；正式内容、飞书与企业系统接入由后续迭代完成
- GitHub：`JzzzX/ai-customer-service-training`（Private）
- 部署：Vercel + Neon 临时演示环境；交付地址为
  `https://ai-customer-service-training.vercel.app`
- AI：`v0.1.0` 使用Mock Provider，真实模型接入延期

## 进度

| Part | 交付物 | 状态 |
|---|---|---|
| 1 | 规格、计划、Roadmap、Next.js基线、GitHub仓库 | 已完成 |
| 2 | MD/XLSX/MM版本化知识编译器 | 已完成 |
| 3 | Neon数据模型与测试账号认证 | 已完成 |
| 4 | 40题知识小测闭环 | 实现完成，内容审核中 |
| 5 | 8场景Mock情景实战 | 已完成 |
| 6 | 管理端、Neon演示数据、Vercel部署与接手审查 | 技术 Demo 已完成 |
| Deferred | 真实AI Provider与可信度验收 | 暂缓 |

## Part 1 检查清单

- [x] 更新产品规格中的知识输入方案
- [x] 落盘完整实施计划
- [x] 创建持续更新的Roadmap
- [x] 建立敏感资料与本地产物忽略规则
- [x] 初始化Next.js与质量工具
- [x] 以测试先行方式实现最小首页
- [x] 通过lint、类型检查、单元测试和构建
- [x] 初始化Git并完成首个提交
- [x] 创建并推送私有GitHub仓库

## Part 1 完成记录

- 完成日期：2026-07-29
- 应用基线提交：`d21aef5`
- 红灯证据：首页测试先因缺少“AI 客服训练”标题失败，完成页面后转绿。
- 自动验证：`pnpm check`
  - ESLint：通过
  - TypeScript：通过
  - Vitest：1个测试文件、1个测试通过
  - Next.js生产构建：通过，首页静态预渲染
- 浏览器验证：桌面与390px窄屏均完成目视检查；双入口清晰，按钮高度48px，
  窄屏无横向溢出。
- 下一入口：Part 2 先建立统一知识单元契约，再逐个实现Markdown、Excel和MM
  适配器及覆盖报告。

## Part 2 检查清单

- [x] 定义统一知识单元、来源定位和编译清单契约
- [x] 实现Markdown标题层级与正文解析
- [x] 实现Excel工作表、行与图片覆盖统计
- [x] 实现MM节点路径与链接解析
- [x] 实现空内容过滤、重复合并、冲突标记和稳定哈希
- [x] 提供 `knowledge:check` 与 `knowledge:publish` 本地命令
- [x] 用当前8份真实源文件验证覆盖门禁
- [x] 通过lint、类型检查、单元测试和生产构建
- [x] 更新完成记录
- [x] 推送 `main`

## Part 2 完成记录

- 完成日期：2026-07-29
- 实现提交：`3f2d0bd`
- 统一知识契约包含稳定ID、内容哈希、语义键、分类路径和可追溯来源定位。
- 支持Markdown、Excel和FreeMind MM；当前真实资料集为6个Markdown和2个
  Excel文件。
- 真实资料覆盖门禁：
  - 8个源文件、9个Excel工作表；
  - 922个原始知识单元，合并15个重复项后得到907个；
  - 133项图片引用跳过并计入覆盖报告；
  - 248个空内容项被过滤；
  - 0个解析错误。
- 版本哈希：`3b96799546b39008a278cd65a13610f57e4d042939f11c8b57b00db7b9c530f7`。
- 幂等验证：首次发布生成3个本地产物，第二次发布未重复写入。
- 自动验证：`pnpm check`
  - ESLint：通过
  - TypeScript：通过
  - Vitest：10个测试文件、22个测试通过
  - Next.js生产构建：通过
- 待知识负责人确认：`企划问答.xlsx` 产品表第67行与第98行的同一问答分别为
  “80mg/100g左右”和“80mg/100g左”；编译器已将其标记为1项冲突，未修改源文件。
- 下一入口：Part 3 建立数据库和测试账号认证，并把已通过门禁的不可变知识版本
  发布到Neon。

## Part 3 检查清单

- [x] 定义用户、知识、题库、场景、训练、报告、复核和分配数据模型
- [x] 生成并审查首个PostgreSQL迁移
- [x] 建立Neon HTTP数据库连接边界
- [x] 实现知识版本事务发布、幂等检查和冲突知识隔离
- [x] 实现预置管理员与学员测试账号认证
- [x] 实现未登录跳转和管理员角色保护
- [x] 提供不含真实密钥的环境变量示例
- [x] 通过lint、类型检查、单元测试和生产构建
- [x] 更新完成记录并推送 `main`

## Part 3 完成记录

- 完成日期：2026-07-29
- 实现提交：`404ad1b`
- 数据模型：16张MVP表、13个PostgreSQL枚举、31个外键，并包含唯一约束、
  范围检查和必要索引。
- 知识版本：通过部分唯一索引保证最多只有一个活动版本；数据库发布使用事务，
  相同哈希保持幂等，冲突知识禁止自动用于小测、场景和评分。
- 认证：Auth.js Credentials + JWT会话，提供管理员和学员种子账号，不开放注册，
  密码只从本地环境变量读取并使用bcrypt哈希。
- 权限：公开页、学员训练区和管理员区分层保护；页面保护与Proxy路由保护同时生效。
- 数据库工具：
  - `pnpm db:generate`
  - `pnpm db:check`
  - `pnpm db:migrate`
  - `pnpm db:seed`
  - `pnpm knowledge:publish:db`
- 自动验证：`pnpm check`
  - ESLint：通过
  - TypeScript：通过
  - Vitest：18个测试文件、41个测试通过
  - Drizzle迁移检查：通过
  - Next.js生产构建：通过
- 本地HTTP冒烟：登录页返回200；未登录访问训练区和管理区均返回307并跳转登录页。
- 当前没有提交Neon连接串，因此未对云端数据库执行迁移和种子；按既定计划在Part 6
  创建Neon环境后执行，代码和迁移已经就绪。
- 下一入口：Part 4 基于题库、题组、作答和知识来源表实现40题知识小测闭环。

## Part 4 检查清单

- [x] 从当前有效知识版本生成40道单选题和判断题草稿
- [x] 每道题绑定知识单元、来源定位、分类和版本
- [x] 自动生成题保持草稿态，管理员审核后才可发布
- [x] 实现逐题作答、即时反馈、解释和知识来源展示
- [x] 实现计分、80%通过线和结果页
- [x] 实现错题重练
- [x] 实现持久化学习历史
- [x] 实现管理员题目与题组基础管理
- [x] 用当前真实知识包完成生成门禁
- [x] 通过lint、类型检查、单元测试和生产构建
- [x] 更新完成记录并推送 `main`

## Part 4 阶段记录

- 更新日期：2026-07-29
- 首个实现提交：`e55e5f2`
- 审题、发布与历史闭环提交：`5b99f02`
- 新增 `pnpm quiz:build`，从当前通过门禁的知识包生成40道可追溯草稿题：
  - 20道单选题、20道判断题；
  - 产品属性及卖点10题、宠物生理和喂养10题、日常问答10题、活动促销1题、
    服务流程与规则9题；
  - 题目版本：`3cde0c419c4935e917f2237552eddce766affba458b8045477fcd0f141d2e991`；
  - 第二次生成保持幂等，没有重复写入本地产物。
- 自动题全部为草稿并绑定知识版本、知识单元和来源定位；冲突知识不会进入候选集。
- 学员端已提供5题交互演示，验证逐题作答、即时反馈、来源说明、80%通过线、结果页
  和错题重练；正式40题在管理员审核前不向学员发布。
- 自动验证：`pnpm check`
  - ESLint：通过
  - TypeScript：通过
  - Vitest：24个测试文件、55个测试通过
  - Drizzle迁移检查：通过
  - Next.js生产构建：通过，新增 `/practice/quiz` 动态路由
- 本地测试账号提交：`43eab05`
  - Neon未配置时，可通过显式开关启用 `.env.local` 中的管理员与学员账号；
  - 该回退在生产环境或存在 `DATABASE_URL` 时强制停用，不改变未来飞书身份接入边界；
  - `.env.local` 已确认被Git忽略。
- 真实浏览器冒烟：
  - 学员账号登录成功并进入训练中心；
  - 完成5题演示小测，故意错1题后得到80%并通过；
  - 即时正确/错误反馈、答案解释和来源说明正常；
  - “重练错题”只返回答错的1题；
  - 390px窄屏无横向溢出。
- 管理员审题与发布：
  - 提供逐题编辑、来源核对、审核进度和前后翻页；
  - 审核记录原子写入本地产物，刷新和重启后仍可恢复；
  - 40题未全部通过时发布按钮保持禁用；全部通过后生成不可变正式题组和唯一发布指针；
  - 学员端优先读取正式题组，每组10题，并平衡为5道单选题和5道判断题。
- 学习历史：
  - 正式小测结束后由服务端重新核验答案，客户端不能直接提交分数；
  - 记录按登录账号隔离并原子保存；训练中心新增简洁的 `/practice/history` 页面；
  - 当前本地测试模式写入不进Git的 `artifacts/quiz`，未来接入飞书身份和正式存储时
    保持服务层接口不变。
- 最新自动验证：`pnpm check`
  - ESLint：通过
  - TypeScript：通过
  - Vitest：31个测试文件、73个测试通过
  - Drizzle迁移检查：通过
  - Next.js生产构建：通过，包含 `/admin/questions`、`/practice/quiz` 和
    `/practice/history` 动态路由。
- 最新真实浏览器验收：
  - 管理员账号看到0/40审核进度和禁用的发布按钮；
  - 第一题精简为四个选项并保存后进入第二题，进度更新为1/40；
  - 学员账号仍只看到5题演示题，不会提前接触未完成审核的题库；
  - 正式题组发布前，练习记录页显示清晰空态；
  - 390px窄屏无横向溢出，浏览器控制台无应用错误。
- 剩余内容工作：39题仍需知识负责人逐题确认，完成后才能点击发布。技术实现已完成，
  Part 4仍保持“内容审核中”，不以自动批量通过绕过人工门禁。

## Part 5 检查清单

- [x] 建立售前、物流、破损少货、客诉各2个固定场景
- [x] 定义 `ConversationProvider` 与 `EvaluationProvider`
- [x] 实现确定性Mock连续对话并明确标识“演示模式”
- [x] 实现会话本地持久化和页面刷新恢复
- [x] 实现场景列表、单场景训练台和最大轮次
- [x] 实现结束训练、五维评分报告和关键风险结论
- [x] 实现报告查看和原场景重练
- [x] 验证8个场景均可独立开始、连续对话、恢复和结束
- [x] 通过lint、类型检查、单元测试和生产构建
- [x] 完成真实浏览器验收并推送 `main`

## Part 5 阶段记录

- 开始日期：2026-07-29
- 首版仅使用确定性Mock Provider，不调用真实模型，所有结果不计入AI可信度验收。
- 继续使用本地测试账号与不进Git的本地产物；未来飞书身份与正式存储通过服务层替换。
- 界面保持单任务训练台，不增加积分、排行榜、复杂导航或大面积数据卡片。
- 8个版本化场景覆盖：
  - 售前：幼犬主粮推荐、价格和赠品异议；
  - 物流：在途长时间未更新、改址与拦截；
  - 破损少货：错发漏发、包装和产品破损；
  - 客诉：突然拒食、食用后呕吐软便。
- `MockConversationProvider` 按场景脚本确定性分块输出顾客回复；
  `MockEvaluationProvider` 根据显式关键词证据计算五维分数，达到80分且无关键风险才通过。
- 会话按账号隔离并原子保存到不进Git的 `artifacts/scenario`；刷新通过会话ID恢复，
  已完成会话不能继续追加消息，达到12轮会自动完成。
- 学员路径：训练中心 → 场景列表 → 开始前说明 → 模拟接待 → 五维报告 → 原场景重练。
- 自动验证：`pnpm check`
  - ESLint：通过
  - TypeScript：通过
  - Vitest：42个测试文件、96个测试通过
  - Drizzle迁移检查：通过
  - Next.js生产构建：通过，新增场景列表、详情、会话和报告动态路由。
- 8场景批量验收：每个场景均成功完成启动、首轮连续对话、重新加载、结束生成报告和
  新会话重练。
- 真实浏览器验收：
  - 学员账号可从训练中心进入8场景列表；
  - 完成一轮售前对话后刷新，消息和1/12进度保持；
  - 主动结束后得到63分“需要重练”五维报告，知识来源和参考回复正常；
  - 重练创建新会话并恢复到0/12轮；
  - 390px窄屏无横向溢出，浏览器控制台无应用错误。
- 实现提交：`f282574`

## Part 6 收口记录

- 完成日期：2026-07-30
- 交付口径：可供产品和技术接手的在线技术 Demo，不宣称为正式培训内容版。
- Vercel Production 部署状态为 `Ready`，远程构建成功，17 个页面完成生成；
  正式别名为 `https://ai-customer-service-training.vercel.app`。
- Neon 作为本次 Demo 的临时持久化设施，已完成迁移、测试账号初始化、1 个活动知识
  版本、40 道草稿题和 8 个已发布 Mock 场景的数据准备。
- 生产运行时只使用数据库 Repository；本地文件 Store 仅保留给显式本地 Demo。
- 已修复 Neon HTTP 驱动不支持交互式事务的问题，改用 WebSocket/Pool 驱动，并通过
  真实 Neon 重复发布验证事务和幂等性。
- 已完成基础架构审查：认证 Provider、知识输入、AI Provider 和持久化均有显式替换
  边界，不把当前 Credentials、Neon、本地文档或 Mock AI 固化为企业最终方案。
- 当前 40 题为 0/40 人工审核，未发布正式题组；学员端只能使用明确标识的演示内容。
- 当前中国大陆网络对 `.vercel.app` 出现 DNS 污染/连接重置，本机无法完成大陆直连
  验收；Vercel 控制面与构建日志均确认部署完成。该限制属于交付风险，不等同于应用
  构建失败。
- 详细交接见 `docs/2026-07-30-PART6-VERCEL-HANDOFF.md`。

## 更新规则

- 每开始一个Part，将其状态改为“进行中”并写明当前目标。
- 每完成一个可独立验证的交付物，勾选检查项并提交。
- 遇到范围、技术或外部依赖变化时，先更新规格和实施计划，再继续编码。
- 每个Part结束时记录验证命令、结果、提交SHA和下一Part入口。

## 决策记录

- 2026-07-29：取消飞书Webhook实时同步，改为本地离线解析和不可变知识版本发布。
- 2026-07-29：首版只处理文字；图片、OCR和多模态延期。
- 2026-07-29：不建设知识上传后台，不使用向量库或开放式RAG。
- 2026-07-29：GitHub私有仓库，主要开发直接进入 `main`；高风险实验使用短期 `feature/*` 分支。
- 2026-07-29：真实源文件不进入Git，规范化知识由本机直接写入Neon。
- 2026-07-29：真实AI接口暂缓，`v0.1.0` 使用明确标识的Mock Provider。
- 2026-07-29：企业知识引擎当前不可稳定接入，MVP恢复本地离线知识方案；后续可将
  企业知识引擎作为独立适配器评估，不阻塞MVP。
- 2026-07-29：当前真实资料集为6个Markdown和2个Excel文件；MM适配器保留并测试，
  但不把不存在的MM文件计入当前资料覆盖门禁。
- 2026-07-29：MVP使用Auth.js Credentials和Neon用户表作为临时身份入口，不引入
  Supabase；后续接入飞书账号时只替换认证Provider与用户映射层，既有角色、训练记录和
  业务数据模型保持不变。
- 2026-07-29：知识题可从有效知识版本辅助生成，但必须先进入草稿态；管理员审核后才可
  发布给学员，避免未经确认的干扰项进入正式训练。
- 2026-07-29：Neon未配置前允许显式启用本地测试账号，以便完成登录和交互验收；此模式
  只在非生产环境且没有数据库连接时生效，不作为正式账号系统。
- 2026-07-29：本地测试模式下，题目审核状态、正式题组指针和按账号练习记录写入
  `artifacts/quiz`；正式部署时通过既有服务层替换为企业存储，不把本地文件方案当成
  最终生产架构。
- 2026-07-30：本周MVP继续使用Vercel + Neon；因当前没有备案域名，不将EdgeOne
  中国大陆正式部署纳入Part 6，EdgeOne保留为备案后或备用部署方案。
- 2026-07-30：Part 6完成原计划中的管理端、数据库持久化、E2E和真实生产部署；
  仅真实AI Provider及其可信度验收继续延期，原规格其他范围外能力不因此进入MVP。
- 2026-07-30：生产环境禁止使用本地文件Store和本地测试账号；Vercel重新部署后，
  知识、题库、练习、场景、分配和复核数据必须由Neon持久化。
- 2026-07-30：40题必须保留人工内容审核门禁；若知识负责人未完成40/40审核，
  可交付技术验收版，但不得宣称为正式培训内容版。
- 2026-07-30：根据交付优先级收缩 Part 6，不再扩展独立账号、复杂数据库方案或
  E2E 基础设施；本次只完成 Vercel 技术 Demo、必要的生产修复和接手审查。
- 2026-07-30：Neon 与 Auth.js Credentials 仅是 Demo 运行设施；后续接入飞书和
  公司系统时，通过现有认证 Provider、用户映射和 Repository 边界替换。
