# 当前项目质量与真实客服使用适用性审核

最后更新：2026-08-11
审核基线：`main` / `ab5609e`

## 审核结论

当前项目已经具备较完整的学员自助训练闭环，代码组织、数据隔离、服务端判分、数据库约束和自动化测试质量均达到继续建设和小范围受控试用的水平。

但它目前**不足以直接作为真实客服人员的正式生产系统**。现阶段适合：

- 内部产品演示；
- 使用 Mock AI 的题库、专题练习和流程试用；
- 在明确负责人、有限账号、可人工兜底的环境中进行受控试点。

现阶段不适合：

- 把现有 Vercel 演示地址当作正式培训环境；
- 保存真实学员的长期训练记录；
- 把 Mock AI 报告当作真实模型能力验收结果；
- 在没有登录防护、备份恢复和监控告警的情况下开放给全量客服；
- 多实例或 Serverless 横向扩容。

正式开放前至少要关闭以下五类缺口：

1. 把应用部署到与 SQLite 架构匹配的单实例持久环境，或先把数据库迁回托管 PostgreSQL；
2. 让部署环境真实访问公司 AI 网关，并完成线上真实 AI 验收；
3. 补齐登录限流、会话撤销和账号停用后的即时失效；
4. 建立自动备份、恢复演练、健康检查、监控和告警；
5. 处理依赖安全告警，并明确主管审核、任务分配和培训运营是否属于正式范围。

## 当前真实运行状态

- Vercel Production：<https://ai-customer-service-training.vercel.app/login>
- 当前配置：`DEMO_MODE=true`、`SCENARIO_AI_MODE=mock`。
- 登录页提供“直接进入演示”，使用固定的“演示学员”身份。
- 数据库是当前函数实例内的内存 SQLite；冷启动、实例切换或重新部署后数据会重置。
- 当前生产地址只能证明页面、Mock 题库和 Mock 情景流程可演示，不能证明正式数据持久化、真实账号体系或真实 AI 可用。

Vercel Function 会按请求调用并根据流量扩缩、缩容到零；这与本项目当前要求的“单一长期运行进程 + 持久本地 SQLite 文件”不一致。现有 Vercel 部署因此必须继续被定义为临时演示，而不是正式生产。

## AI 模拟对话现状声明

### 已知事实

- 本地 `localhost` 连接公司 OpenAI 兼容网关可以正常完成真实 AI 请求；这是用户确认且项目在 2026-08-03 留有实测记录的现状，本轮审核没有重新消耗公司模型额度复测。
- Vercel 上的真实 AI 尚不可用。既有实测中，本机通过 VPN 可访问网关，但 Vercel `hkg1` 函数无法访问该私有网络路径，最终请求超时。
- 这不是“Vercel 不能调用 AI API”的通用产品限制，而是**Vercel 运行网络无法到达公司 AI 网关**的部署拓扑问题。
- 当前 Vercel 已切为 Mock AI，因此演示流程可用不代表真实 AI 问题已解决。

### 正式验收口径

部署后的真实 AI 至少要完成：

1. 使用部署环境的真实账号发起训练；
2. 连续完成至少 3 轮真实 AI 对话；
3. 刷新页面后恢复完整会话；
4. 成功生成真实 AI 评分报告；
5. 验证超时、网关不可达、空回复和重复提交时的用户提示；
6. 验证并发、调用额度、超时和日志中不泄露密钥或内部网络信息。

在上述验收完成前，页面必须清楚标记 Mock/演示模式，不能把 Mock 分数用于员工绩效、认证或正式培训结论。

## 代码与架构审核

| 维度 | 当前证据 | 判定 |
|---|---|---|
| 核心训练闭环 | 登录、专题题库、正式题、情景会话、刷新恢复、报告和个人历史均有实现 | 良好，可继续建设 |
| 数据隔离 | 小测和情景 Repository 查询同时约束 `learnerId` 与记录 ID；E2E 验证跨学员历史隔离 | 良好 |
| 判分与输入校验 | 正确答案在服务端复核；关键输入使用 Zod；SQLite 有外键、唯一约束和事务 | 良好 |
| 内容版本 | 知识、题组、场景均保留发布状态和版本引用，历史训练绑定当时版本 | 良好 |
| 故障处理 | AI 基础设施错误会脱敏；写入有事务和重复提交防护 | 基础合格，但缺监控平台和告警 |
| 自动化验证 | 单元/组件测试、Mock E2E、构建、Schema check、SQLite 并发脚本齐全 | 良好，但真实 AI 与长期压力测试未完成 |
| 身份安全 | 密码使用 bcrypt cost 12；停用账号不能新登录 | 不足：无登录限流/锁定；JWT 不会在每次请求重新检查 `isActive`，已登录账号停用或改密后不能立即撤销现有会话 |
| 数据持久化 | 持久模式使用 WAL、`busy_timeout`、完整性和外键检查 | 只适合单实例；当前 Vercel 演示不持久；尚无自动备份和恢复演练证据 |
| 运营能力 | 账号、内容发布、停用、重置密码均通过 CLI | 适合技术团队维护的小试点；没有网页管理端、任务分配、主管复核、组织统计或审计后台 |
| 可观测性 | 运行时错误输出结构化基础字段 | 不足：没有正式健康/就绪接口、集中日志、指标、告警和容量趋势 |
| 依赖安全 | 本轮 `pnpm audit --prod --audit-level moderate` | 未达上线门槛：共 10 个告警，7 high、3 moderate；主要来自 Next 的 `sharp`/`postcss` 传递依赖和 `exceljs` 依赖链，需升级验证或形成风险缓解记录 |
| 文档一致性 | `README.md`、`docs/DEPLOYMENT.md` 已描述学员 SQLite 版本 | 存在漂移：`docs/MVP-ACCEPTANCE.md` 仍描述旧 Neon/管理员版本，不应作为当前上线依据 |
| 仓库可维护性 | 当前运行入口清晰位于根 Next.js 项目 | `backend/`、`frontend/`、`deploy/` 仍是历史迁移代码，容易误导接手者，应继续明确其非生产身份或后续归档 |

## 本轮验证证据

### 已通过

- `pnpm check`
  - ESLint 通过；
  - TypeScript 通过；
  - Vitest：64 个文件、191 个测试通过；
  - Drizzle migration check 通过；
  - Next.js production build 通过。
- `pnpm test:e2e`
  - 3 个 Mock 浏览器测试通过；
  - 覆盖正常学员、停用账号、跨学员历史隔离、三轮情景、刷新恢复和报告生成。
- `pnpm e2e:prepare && pnpm test:sqlite:concurrency`
  - 30 workers、1500 条真实并发写入通过；
  - 无锁失败、重复成绩/消息或外键损坏。

### 验证限制与发现

- 当前本机是 Node.js `26.6.0`，项目声明 Node.js `24.x`；本轮命令虽通过，但正式部署仍应在 Node.js 24 重新跑完整门禁。
- 直接运行文档中的 `pnpm test:sqlite:concurrency` 会因临时库没有 schema 而报 `no such table: users`；先执行 `pnpm e2e:prepare` 后通过。当前脚本和文档缺少自包含的建库步骤，应在后续修正。
- 并发验证只有约 3 秒，不等于 30 人真实混合业务负载或长时间稳定性验收；上线前还需在目标服务器运行 10 分钟以上 soak，并同时覆盖登录、读题、提交、对话和报告。
- 本轮未执行 `pnpm test:e2e:live`，没有重新调用公司真实 AI。
- 本轮依赖审计发现 7 high、3 moderate 告警，不能以功能测试通过替代安全升级评估。

## 当前最适合的部署路线

### 路线 A：公司 Linux 单实例，保留 SQLite（建议用于首批真实试点）

```mermaid
flowchart LR
  Feishu["飞书工作台 / 浏览器"] --> Nginx["公司域名 + HTTPS + Nginx"]
  Nginx --> Next["单个 Next.js Node 24 实例"]
  Next --> SQLite["持久磁盘 SQLite"]
  Next --> Gateway["公司 AI 网关"]
  SQLite --> Backup["自动备份 + 异地副本 + 恢复演练"]
```

这条路线最符合当前代码：不改数据库访问层，部署环境也更容易复用公司网络/VPN访问 AI 网关。前提是明确单实例所有权、持久磁盘、TLS、进程守护、备份、监控、容量和故障恢复负责人。

它适合作为有限规模首批试点，不应承诺无停机横向扩容。项目预期约 100 个账号、30 人同时在线，目前只有短时写入烟测，还需要目标服务器上的真实混合负载验证。

### 路线 B：继续使用 Vercel，数据库改为托管 PostgreSQL

如公司希望保留 Vercel 的托管与弹性，需要先移除运行时本地 SQLite 依赖，将数据层迁移到 Vercel 函数可达的托管 PostgreSQL；同时为公司 AI 网关提供受控的公网 HTTPS 443 入口、固定出口 IP 白名单或企业级私网连接。

这条路线改造和验收成本更高，但更适合多实例与后续扩容。数据库迁移和 AI 网络打通是两个独立项目，不能只完成其中一个就宣布上线。

## 飞书接入与部署后的设计

飞书 OAuth 应被定位为“企业身份入口”，不能替代业务授权、账号生命周期和部署网络。

### 推荐身份链路

1. 用户从飞书工作台或网页登录；
2. 服务端生成并校验 OAuth `state`，接收一次性授权码；
3. 授权码只交给服务端换取 `user_access_token`；
4. 服务端读取用户身份，并绑定到本项目内部 `user.id`；
5. 训练记录继续只使用内部 `user.id` 作为稳定外键；
6. 员工离职、停用或组织调整时，同步停用账号并撤销现有会话。

飞书官方授权码只有短期有效且只能使用一次；令牌和 App Secret 必须留在服务端，并遵循最小权限原则。

### 当前数据模型要先调整

当前 `users.passwordHash` 为必填且用户只通过 email 唯一识别，不适合直接把飞书邮箱当永久身份键。接入前建议新增独立身份表，例如：

```text
auth_identities
- id
- user_id
- provider              # feishu / credentials
- provider_subject      # 经公司确认的 open_id、union_id、user_id 或 SSO subject
- tenant_key
- created_at
- last_login_at
```

具体采用 `open_id`、`union_id`、`user_id` 还是统一 SSO subject，需要根据飞书应用类型、企业租户范围和未来多应用复用需求由公司确认；不要默认用邮箱做永久关联。首次绑定、重复身份、换邮箱、离职停用和误绑回滚都要有明确规则。

### 飞书上线不应遗漏

- 应用可见范围只控制谁能看到入口，不能替代项目内账号状态和权限判断；
- OAuth 回调域名必须使用正式 HTTPS 域名；
- App ID、App Secret、`AUTH_SECRET` 和 AI 网关密钥由部署环境托管，不进入 Git；
- 登录成功后仍要执行项目内 `isActive`、组织范围和角色检查；
- 应记录登录、账号绑定、停用、内容发布和关键训练操作的审计事件；
- 如果未来恢复主管/管理员能力，应单独设计角色与数据范围，不要仅根据飞书部门名称硬编码权限。

## 正式上线门禁

### P0：未完成不得给真实客服长期使用

- [ ] 决定并落地路线 A 或路线 B；正式环境不得使用 `DEMO_MODE=true`。
- [ ] 正式数据可持久写入，完成自动备份和至少一次恢复演练。
- [ ] 部署环境完成真实 AI 三轮对话、刷新恢复和报告验收。
- [ ] 增加登录限流/锁定策略，并让停用账号、重置密码和管理员撤销可使现有会话及时失效。
- [ ] 关闭依赖安全 high 告警，或由安全负责人书面确认不可利用条件和临时缓解措施。
- [ ] 在 Node.js 24 目标环境重新运行完整门禁。

### P1：首批受控试点前完成

- [ ] 增加健康/就绪检查、集中日志、错误告警、磁盘容量和备份失败告警。
- [ ] 在目标服务器完成至少 10 分钟混合负载 soak，验证 30 人目标并发。
- [ ] 确认 AI 网关的数据分类、日志留存、提示词/对话内容是否允许传输及脱敏要求。
- [ ] 明确内容发布责任人、题目/场景质量复核方式和异常人工处理流程。
- [ ] 修正并发脚本的建库前置条件，并清理 `docs/MVP-ACCEPTANCE.md` 的旧架构描述。

### P2：扩大使用范围前完成

- [ ] 接入飞书 OAuth 和组织生命周期同步。
- [ ] 根据业务需要增加主管看板、任务分配、人工复核、组织统计和审计查询。
- [ ] 评估 SQLite 容量上限；需要多实例、高可用或更大规模时迁移 PostgreSQL。
- [ ] 归档历史 `backend/`、`frontend/`、`deploy/` 路线，减少维护误导。

## 产品适用性判断

如果目标只是让客服人员自行刷题、完成模拟接待并查看个人报告，当前产品形态已经接近可试点，主要工作是补齐生产基础设施与安全门禁。

如果目标还包括主管布置训练、查看团队结果、人工复核 AI 评分、追踪组织完成率或把训练结果用于绩效，当前学员轻量版的业务功能仍不完整，需要先确认正式产品范围，再恢复或重建对应管理能力。

## 审核依据与后续参考

- 当前代码：`src/auth.ts`、`src/lib/auth/`、`src/db/`、`src/lib/scenario/`、`src/app/practice/`
- 当前部署边界：`README.md`、`docs/DEPLOYMENT.md`
- 飞书授权码：<https://open.feishu.cn/document/common-capabilities/sso/api/obtain-oauth-code>
- 飞书 `user_access_token`：<https://open.feishu.cn/document/authentication-management/access-token/get-user-access-token>
- Vercel Function 生命周期：<https://vercel.com/docs/functions>

> `docs/MVP-ACCEPTANCE.md` 仍包含旧 Neon、管理员和历史 Vercel 验收信息。本文件与当前 `README.md`、`docs/DEPLOYMENT.md` 应作为 2026-08-11 的当前判断依据。
