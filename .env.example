# SQLite database file. Use a persistent, writable local disk and one Node.js instance;
# Vercel/Serverless and horizontally scaled instances are not supported.
SQLITE_PATH=./data/training.sqlite

# 仅用于临时预览：开启后使用内存 SQLite 和“直接进入演示”入口；默认关闭。
DEMO_MODE=false

# Required in production. Generate at least 32 random characters.
AUTH_SECRET=

# 正式环境仅使用飞书 OAuth 登录。
# 飞书开放平台回调地址配置为：
# https://<正式域名>:<端口>/api/auth/callback/feishu
FEISHU_APP_CLIENT_ID=
FEISHU_APP_CLIENT_SECRET=

# 正式环境不提供邮箱/密码登录。
# 培训账号通过 users + feishu_identities 预分配并由飞书身份验证。

# AI 模拟情景对话模式：mock（默认，确定性演示）| real（真实 LLM）
SCENARIO_AI_MODE=mock

# 真实模式必需：从 Vercel 函数区域可达的 OpenAI 兼容 API 配置
# baseURL 使用公司批准的 OpenAI 兼容入口；SDK 会自动拼接对应的 completions 路径
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.example.com/v1
OPENAI_MODEL=your-approved-model

# 如果改用 Vercel AI Gateway，可显式设为 true；该链路可能要求项目账户具备额度。
# VERCEL_OIDC_TOKEN 由 Vercel 运行时自动注入，不要手动保存。
AI_GATEWAY_ENABLED=
AI_GATEWAY_MODEL=bytedance/seed-1.8
